#### Step

```
$ terraform init # init, download provider
$ terraform plan # show the plan
$ terraform apply # apply that plan
$ terraform show # show current plan
$ terraform destroy # destroy current infrastructure in show
```